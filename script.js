//your JS code here. If required.
const student = {
     name: xyz
}

Array.prototype.getKeys = function (fn){
      const result = [];

      for(let i=0; i<getKeys.length; i++){
          result.push(fn(getKeys[i], i ,getKeys))
      }

      return result
}

const result = student.getKeys()
console.log(result)